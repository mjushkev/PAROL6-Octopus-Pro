# Waldo Commander integration staging

The immutable baseline is recorded in `THIRD_PARTY_LOCK.md`. Phase 6 import and
modification has not begun. Use `scripts/fetch-upstreams.ps1` to acquire and
verify the reviewed source without making network access part of runtime.

