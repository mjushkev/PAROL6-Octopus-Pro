# Configuration schemas

Machine-validatable calibration and hardware schemas will be added before
Phase 2 configuration storage is implemented. Until then, every hardware field
in the default configuration remains `UNVERIFIED` and outputs remain disabled.

